// number -> integer or decimal numbers

//Typescript supports Type Inference -> It automatically detects the data type of a variable based on the value assigned to it

let salary:number=-10000.34234;

console.log(typeof salary); // number
let max_range =Number.MAX_VALUE;
let min_range =Number.MIN_VALUE;

console.log(max_range);
console.log(min_range);